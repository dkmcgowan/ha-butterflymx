{
  "config": {
    "step": {
      "credentials": {
        "title": "ButterflyMX API credentials",
        "description": "This copy of the integration does not ship a client ID, so you need one of your own from ButterflyMX's developer programme. Leave the client secret empty unless you were given one - most ButterflyMX credentials do not have a secret and authorize with PKCE instead. Leave the redirect URI at its default unless ButterflyMX registered a custom one for your application.",
        "data": {
          "client_id": "Client ID",
          "client_secret": "Client secret (only if you were given one)",
          "redirect_uri": "Redirect URI"
        }
      },
      "authorize": {
        "title": "Link your ButterflyMX account",
        "description": "1. Open this link and sign in with your ButterflyMX account:\n\n{authorize_url}\n\n2. Approve access.\n3. ButterflyMX will show you an authorization code (or send you to {redirect_uri} with a `code` parameter).\n4. Paste the code - or the whole URL you landed on - below.\n\nThe code is single use and expires quickly, so paste it promptly.",
        "data": {
          "auth_code": "Authorization code"
        }
      },
      "reauth_confirm": {
        "title": "Reconnect ButterflyMX",
        "description": "The stored ButterflyMX authorization for client `{account}` is no longer valid. Continue to link the account again."
      }
    },
    "error": {
      "cannot_connect": "Could not reach ButterflyMX. Check your network and the endpoint URLs.",
      "invalid_auth": "ButterflyMX rejected these credentials or this authorization code.",
      "invalid_auth_code": "That does not look like an authorization code.",
      "unknown": "Unexpected error. Check the Home Assistant log for details."
    },
    "abort": {
      "already_configured": "This ButterflyMX account is already set up.",
      "reauth_successful": "ButterflyMX was reconnected successfully.",
      "wrong_account": "Those credentials are for a different ButterflyMX account."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "ButterflyMX options",
        "description": "Polling is the default and works everywhere.\n\nPush needs Home Assistant to be reachable from the internet. Turning it on registers this URL with ButterflyMX:\n\n{webhook_url}\n\nCheck that address is reachable before enabling it. Polling does not stop when push is on, it only slows down: a call delivered while Home Assistant is restarting is gone for good, so the slow poll stays on to catch anything push misses. If doorbells stop arriving, turn push off and confirm polling still works - see the README for how to check.",
        "data": {
          "call_scan_interval": "Call polling interval",
          "relock_delay": "Show door as unlocked for",
          "enable_webhook": "Also let ButterflyMX push calls (needs an externally reachable Home Assistant)"
        }
      }
    }
  },
  "entity": {
    "event": {
      "doorbell": {
        "name": "Doorbell",
        "state_attributes": {
          "event_type": {
            "state": {
              "call": "Call"
            }
          }
        }
      }
    },
    "image": {
      "last_call_snapshot": {
        "name": "Last call snapshot"
      }
    },
    "sensor": {
      "last_call": {
        "name": "Last call"
      }
    }
  },
  "exceptions": {
    "door_release_failed": {
      "message": "Could not open {door}: {error}"
    }
  }
}
